--- a/PKGBUILD
+++ b/PKGBUILD
@@ -24,7 +24,6 @@
 )
 makedepends=(
   docbook-xsl
-  doxygen
   git
   meson
   python
@@ -43,6 +42,11 @@
   ln -s ../xmlconf
 }
 
+prepare() {
+  # kashira: no doxygen in the tree; doc subdir hard-requires it
+  sed -i "/subdir('doc')/d" libxml2/meson.build
+}
+
 build() {
   local meson_options=(
     -D icu=enabled
