--- a/PKGBUILD
+++ b/PKGBUILD
@@ -17,7 +17,8 @@
          libgcc
          libstdc++
          libxml2)
-makedepends=(cmake 
+makedepends=('gcc'  # kashira: libstdc++ headers (no gcc in base-devel)
+            cmake 
              doxygen
              gi-docgen
              git
