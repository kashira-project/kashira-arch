--- a/PKGBUILD
+++ b/PKGBUILD
@@ -21,6 +21,7 @@
 }
 
 build() {
+  CXXFLAGS+=' -std=c++14'
   cd ${pkgname}-${pkgver}
   ./configure --prefix=/usr
   sed -i -e 's/ -shared / -Wl,-O1,--as-needed\0/g' libtool
