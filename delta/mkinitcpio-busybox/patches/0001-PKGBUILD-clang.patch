--- a/PKGBUILD
+++ b/PKGBUILD
@@ -31,7 +31,7 @@
 build() {
   # reproducible build
   export KCONFIG_NOTIMESTAMP=1
-  make -C "busybox-$pkgver"
+  make CC=clang HOSTCC=clang -C "busybox-$pkgver"  # kashira: busybox Makefile + host tools default to gcc
 }
 
 package() {
