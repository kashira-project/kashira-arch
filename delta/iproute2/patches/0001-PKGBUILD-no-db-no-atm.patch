--- a/PKGBUILD
+++ b/PKGBUILD
@@ -14,7 +14,7 @@
          'libcap' 'libcap.so'
          'libelf'
          'libbpf' 'libbpf.so')
-makedepends=('git' 'db' 'linux-atm' 'iptables')
+makedepends=('git' 'iptables' 'gcc')  # kashira: no db(AGPLv3)/linux-atm; gcc for netem's hardcoded cc
 optdepends=('db: userspace arp daemon'
             'linux-atm: ATM support'
             'python: for routel')
