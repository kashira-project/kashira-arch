--- a/PKGBUILD
+++ b/PKGBUILD
@@ -23,8 +23,8 @@
 }
 
 package() {
-  # libglvnd needs mesa for indirect rendering
-  depends=('libxext' 'mesa' 'opengl-driver')
+  # kashira bootstrap: mesa dep restored after mesa is built
+  depends=('libxext')
 
   meson install -C build --destdir "$pkgdir"
 
